import React, { Component } from "react";

export default class RedPaddle extends Component {
  constructor(args) {
    super(args);
  }

  spawnParticles() {
    for (let i = 0; i < 500; i++) {}
  }
}
